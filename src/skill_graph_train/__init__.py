"""Skill-graph **training** utilities (episodes, checkpoints, optional graph evolution).

Not used by the LangGraph ``run_workflow`` solver. Import ``skill_graph_train`` with repo ``src/`` on
``PYTHONPATH`` (see :func:`skill_graph_train.bootstrap.ensure_import_paths`) or set ``PYTHONPATH=src``.

Canonical episode API: :func:`skill_graph_train.pipeline.run_episode`.
"""

__version__ = "0.2.0"
